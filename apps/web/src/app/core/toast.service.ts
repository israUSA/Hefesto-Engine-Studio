/**
 * Re-exported from @hefesto/web-ui, where it lives next to <hf-toast-outlet>
 * (the outlet needs the service too, and the ui lib can't depend on apps/web).
 */
export { ToastService, type Toast, type ToastOptions } from '@hefesto/web-ui';
